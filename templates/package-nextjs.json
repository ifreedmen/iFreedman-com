{
  "name": "nextjs-template",
  "version": "1.0.0",
  "description": "Next.js template for iFreedmen Creative Suite",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "export": "next build && next export",
    "preview": "npx serve out -l 4000",
    "clean": "rm -rf .next out dist"
  },
  "dependencies": {
    "next": "^14.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "serve": "^14.2.1"
  }
}